# frozen_string_literal: true

RSpec.describe Mel::Scene::Fsm do
  it "has a version number" do
    expect(Mel::Scene::Fsm::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
